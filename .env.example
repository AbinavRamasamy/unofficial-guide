# Copy this file to .env and fill in real values.
# .env is listed in .gitignore — never commit your real keys.

# Groq API key — get a free key (no credit card) at https://console.groq.com
GROQ_API_KEY=your_key_here
